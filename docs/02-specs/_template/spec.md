# 技术规格模板

> 使用说明：复制此模板到 `02-specs/{feature}/spec.md`，填充实际内容后删除此说明。

---
**版本信息**

| 项目 | 值 |
|------|------|
| 功能版本 | v1.0.0 |
| 适用系统版本 | ≥ v1.0.0 |
| 文档状态 | Draft / Review / Approved |

**版本历史**

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2026-05-27 | 初始版本 | @ |

---

## 概述
<!-- 一句话描述技术方案 -->

## 设计决策
<!-- 关键技术选型及理由 -->

| 决策点 | 方案 | 理由 |
|--------|------|------|
|        |      |      |

## 系统设计

### 架构图
<!-- 嵌入架构图或链接 -->

### 模块划分

| 模块 | 职责 | 依赖 |
|------|------|------|
|      |      |      |

---

## 数据库设计

### ER 图
<!-- 实体关系图，可用工具生成或手绘 -->

```
┌─────────────┐       ┌─────────────┐
│   表A       │ ────▶ │   表B       │
└─────────────┘       └─────────────┘
```

### 表结构设计

#### 表 1: {table_name}

**表说明**:

| 字段名 | 类型 | 是否必填 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| id | BIGINT | 是 | 自增 | 主键 |
| | | | | |
| created_at | TIMESTAMP | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 是 | CURRENT_TIMESTAMP | 更新时间 |

**建表语句**:
```sql
CREATE TABLE {table_name} (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    -- 字段定义

    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='表说明';
```

#### 表 2: {table_name}

**表说明**:

| 字段名 | 类型 | 是否必填 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| | | | | |

**建表语句**:
```sql

```

### 索引设计

| 表名 | 索引名 | 字段 | 类型 | 用途说明 |
|------|--------|------|------|----------|
| | PRIMARY | id | 主键 | |
| | idx_xxx | | 普通索引 | |
| | uk_xxx | | 唯一索引 | |

**索引语句**:
```sql
-- 添加索引示例
CREATE INDEX idx_xxx ON table_name(column_name);
CREATE UNIQUE INDEX uk_xxx ON table_name(column_name);
```

### 数据迁移

**新增表迁移**:
```sql
-- migration/001_create_xxx_table.sql
-- 创建表语句
```

**存量数据处理**:
```sql
-- 数据迁移脚本（如有存量数据需要处理）
```

**回滚脚本**:
```sql
-- 回滚语句
DROP TABLE IF EXISTS {table_name};
```

---

## API 设计

见 [api.md](./api.md)

### 接口清单

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| | GET | /api/v1/xxx | |
| | POST | /api/v1/xxx | |
| | PUT | /api/v1/xxx | |
| | DELETE | /api/v1/xxx | |

---

## 实现细节

### 模块 A: {模块名称}

**职责**:

**接口定义**:
```typescript

```

**核心逻辑**:
```typescript

```

### 模块 B: {模块名称}

**职责**:

**接口定义**:
```typescript

```

**核心逻辑**:
```typescript

```

---

## 状态管理
<!-- 前端状态管理方案，如适用 -->

**状态结构**:
```typescript
interface State {
  // 状态定义
}
```

**状态流转**:
```
初始状态 → 中间状态 → 最终状态
```

---

## 数据流
<!-- 数据如何在模块间流转 -->

```
用户输入 → 校验 → 处理 → 存储 → 响应
```

---

## 缓存策略

| 数据类型 | 缓存位置 | Key 格式 | 过期时间 | 更新策略 |
|----------|----------|----------|----------|----------|
| | Redis | xxx:{id} | 5min | 写入时更新 |

---

## 测试策略

### 单元测试
- 覆盖范围:
- Mock 策略:
- 目标覆盖率: 80%

### 集成测试
- 测试场景:
- 测试数据:

### E2E 测试
- 关键流程:
- 测试环境:

### 测试用例
详见 [test-case.md](./test-case.md)

| 用例类型 | 用例数 | 优先级分布 |
|----------|--------|------------|
| 功能测试 | | P0: P1: P2: |
| 边界测试 | | |
| 异常测试 | | |
| 接口测试 | | |
| 性能测试 | | |

---

## 性能考量

| 指标 | 目标 | 实现方式 |
|------|------|----------|
| 响应时间 | < 200ms | |
| 并发数 | 1000 QPS | |
| 数据量 | 100万 | |

**性能优化点**:
-
-

---

## 安全设计

| 风险点 | 防护措施 |
|--------|----------|
| SQL 注入 | 使用参数化查询 |
| XSS | 输入过滤 + 输出转义 |
| 越权访问 | 权限校验 |

---

## 风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| | 高/中/低 | 高/中/低 | |

---

## 上线检查清单

- [ ] 代码审查通过
- [ ] 测试用例编写完成
- [ ] P0/P1 用例 100% 通过
- [ ] 测试覆盖率 ≥ 80%
- [ ] 性能测试通过
- [ ] 安全审查通过
- [ ] 文档更新完成
- [ ] 配置项确认
- [ ] 回滚方案就绪
- [ ] 数据库迁移脚本准备就绪

---

## 监控告警

| 指标 | 阈值 | 告警级别 |
|------|------|----------|
| 接口响应时间 | > 500ms | Warning |
| 错误率 | > 1% | Critical |

---

## 关联文档

- PRD: [01-prd/{feature}/prd.md](../../01-prd/{feature}/prd.md)
- 测试用例: [test-case.md](./test-case.md)
- 决策记录: [04-decisions/0001-xxx.md](../../04-decisions/0001-xxx.md)
- 数据库迁移: [migration/](../../migration/)

---

**文档状态**: Draft / Review / Approved / Implemented
**负责人**: @
**创建日期**: 2026-05-27
**更新日期**: 2026-05-27

# API 文档模板

> 使用说明：复制此模板到 `02-specs/{feature}/api.md`，填充实际内容后删除此说明。

---
**版本信息**

| 项目 | 值 |
|------|------|
| 功能版本 | v1.0.0 |
| 适用系统版本 | ≥ v1.0.0 |

**版本历史**

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2026-05-27 | 初始版本 | @ |

---

## 概述
<!-- API 模块概述 -->

## 认证方式

### 认证类型
- 方式: Bearer Token / API Key / OAuth 2.0
- Header: `Authorization: Bearer <token>`

### 获取 Token
```bash
# 获取 Token 示例
curl -X POST /api/auth/token \
  -H "Content-Type: application/json" \
  -d '{"key": "<value>"}'
```

## 公共参数

### 请求头
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| Content-Type | string | 是 | application/json |
| Authorization | string | 是 | Bearer Token |

### 分页参数
| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| page | int | 1 | 页码 |
| pageSize | int | 20 | 每页数量，最大 100 |

### 响应格式
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "meta": {
    "page": 1,
    "pageSize": 20,
    "total": 100
  }
}
```

---

## 接口列表

### 1. {接口名称}

**描述**:

**请求**:
```
GET /api/v1/resource/{id}
```

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | string | 是 | 资源ID |

**查询参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|        |      |      |      |

**请求示例**:
```bash
curl -X GET "/api/v1/resource/{id}" \
  -H "Authorization: Bearer <token>"
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误码**:
| 错误码 | 说明 |
|--------|------|
| 400 | 参数错误 |
| 401 | 未授权 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

### 2. {接口名称}

**描述**:

**请求**:
```
POST /api/v1/resource
```

**请求体**:
```json
{
  "field": "value"
}
```

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
|        |      |      |      |

**请求示例**:
```bash
curl -X POST "/api/v1/resource" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{}'
```

**响应示例**:
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误码**:
| 错误码 | 说明 |
|--------|------|
| 400 | 参数错误 |
| 401 | 未授权 |
| 409 | 资源已存在 |

---

## 错误处理

### 错误响应格式
```json
{
  "code": 400,
  "message": "参数错误",
  "errors": [
    {
      "field": "fieldName",
      "message": "错误描述"
    }
  ]
}
```

### 通用错误码

| 错误码 | 说明 |
|--------|------|
| 400 | 请求参数错误 |
| 401 | 未授权，Token 无效或过期 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 429 | 请求频率超限 |
| 500 | 服务器内部错误 |
| 503 | 服务不可用 |

---

## 速率限制

| 接口类型 | 限制 | 窗口 |
|----------|------|------|
| 普通接口 | 100 次 | 1 分钟 |
| 写接口 | 30 次 | 1 分钟 |

---

## 变更日志

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0.0 | 2026-05-27 | 初始版本 |

---
最后更新: 2026-05-27
